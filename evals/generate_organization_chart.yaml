name: generate_organization_chart handles empty input gracefully
server:
  command: node
  args:
  - build/index.js
assert:
  tool: generate_organization_chart
  args:
    data: {}
  expect:
    is_error: true
timeout: 30s
