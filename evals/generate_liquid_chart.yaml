name: generate_liquid_chart returns expected result
server:
  command: node
  args:
  - build/index.js
assert:
  tool: generate_liquid_chart
  args:
    percent: 1
  expect:
    not_error: true
timeout: 30s
