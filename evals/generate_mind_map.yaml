name: generate_mind_map handles empty input gracefully
server:
  command: node
  args:
  - build/index.js
assert:
  tool: generate_mind_map
  args:
    data: {}
  expect:
    is_error: true
timeout: 30s
